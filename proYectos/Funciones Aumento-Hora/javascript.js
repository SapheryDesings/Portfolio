const recurso1Base = 500;
var aumentoRecurso = 0;

function aumentoRecurso (recurso1Base){
	let aumentoRecurso;
	aumentoRecurso = base + aumentoRecurso; 
	return aumentoRecurso;
}

function actualizacionRecurso (aumentoRecurso){
	let recursoActualizado;
	recursoActualizado = recursoActualizado + aumentoRecurso;
	return recursoActualizado;
} 